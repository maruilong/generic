一个元组类库

仅一次方法调用就能返回多个对象,可以return语句只允许返回单个对象,因此,解决办法就是创建一个对象,用它来持有想要返回的多个对象,
我们可以使用泛型,一次性解决这种问题,同时我们在编译器就能确保类型的安全

这个概念成为元组,他是将一组对象直接打包储存于其中的一个单一对象,这个容器对象允许读取其中元素,但是不允许向其中存放新的对象
(这个概念也成为数据传送对象或信使)

通常,元组可以用任意长度,同时,元组中的对象可以是任意不同的类型,不过,我们希望能够为每一个对象指明其类型,
并且从容器中读取出来时,能够得到正确的类型,要处理不同长度的问题,我们需要创建多个不同的元组

也可以通过继承机制来实现长度更长的元组




